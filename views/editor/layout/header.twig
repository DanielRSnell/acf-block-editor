<div class="editor-header">
    <div class="editor-logo">
        <ion-icon name="cube-outline"></ion-icon>
        <span>{{ block_title }}</span>
    </div>
    <div class="breakpoint-controls">
        {% for breakpoint in breakpoints %}
            <button type="button"
                    class="breakpoint-button"
                    data-breakpoint="{{ breakpoint.id }}"
                    title="{{ breakpoint.name }} ({{ breakpoint.width }}px)">
                <ion-icon name="{{ breakpoint.icon }}"></ion-icon>
            </button>
        {% endfor %}
        <button type="button" class="breakpoint-settings" title="Breakpoint Settings">
            <ion-icon name="settings-outline"></ion-icon>
        </button>
    </div>
    <div class="global-controls">
        <button type="button" class="global-save-button" id="global-save-button" title="Save All Changes">
            <ion-icon name="save"></ion-icon>
        </button>
    </div>
</div>

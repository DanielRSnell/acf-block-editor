{
    "name": "your-vendor/client-blocks",
    "description": "Custom blocks manager with PHP, template, JS, and CSS support",
    "type": "wordpress-plugin",
    "require": {
        "timber/timber": "^2.0",
        "composer/installers": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "ClientBlocks\\": "src/"
        }
    },
    "config": {
        "allow-plugins": {
            "composer/installers": true
        }
    }
}